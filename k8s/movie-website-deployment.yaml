apiVersion: apps/v1
kind: Deployment
metadata:
  name: movie-website
  labels:
    frontendapp: moviewebapp
spec:
  replicas: 1
  selector:
    matchLabels:
      frontendapp: moviewebapp
  template:
    metadata:
      labels:
        frontendapp: moviewebapp
    spec:
      containers:
        - name: movie-website-app
          image: MOVIE_WEBSITE_IMAGE
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80

---
apiVersion: v1
kind: Service
metadata:
  name: movie-website-service
spec:
  type: ClusterIP
  selector:
    frontendapp: moviewebapp
  ports:
    - port: 80
      targetPort: 80
